function navigateToDetails(page) {
    window.location.href = page;
  }
  
  function highlightBook(element) {
    element.classList.add('highlight');
  }
  
  function removeHighlight(element) {
    element.classList.remove('highlight');
  }